# Optional agent/tool integrations

These tools may improve coding-agent work but are not required to run ProjectPins.

Canonical product state remains in `docs/project/*` and current phase docs.

Use the smallest tool that answers the current question.
