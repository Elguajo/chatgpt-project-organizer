# Tooling Policy

- Correctness and privacy outrank token savings.
- Use primary/current docs for browser APIs and fast-changing dependencies.
- Avoid duplicate discovery with several tools for the same question.
- Optional agent integrations must not become a project source of truth.
- If an optional tool is unavailable, use native targeted tools and continue.
- Do not install global hooks without understanding their effect.
