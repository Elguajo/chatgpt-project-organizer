# Phases

Phase files use `NN-kebab-name.md`.

`docs/project/ROADMAP.md` is the only current-phase pointer.

A phase is complete only when every acceptance criterion is verified. Authenticated browser QA that cannot be run is an external blocker, not an implicit pass.
