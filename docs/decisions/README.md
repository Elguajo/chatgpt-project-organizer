# Architecture Decision Records

Create an ADR only for consequential choices that future agents might otherwise undo.

Current ADRs:

- ADR-001 — local-first, no backend
- ADR-002 — synthetic Pinned section instead of moving native nodes
- ADR-003 — WXT + vanilla TypeScript
- ADR-004 — DOM adapter with fail-closed behavior
