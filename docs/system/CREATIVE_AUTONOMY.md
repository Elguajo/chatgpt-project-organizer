# Creative Autonomy

The agent may improve unspecified microcopy, spacing, hover/focus behavior, error states, internal naming, and code organization.

The agent may not silently add:

- folders/tags/search/sync;
- backend accounts;
- analytics;
- advertising;
- remote configuration;
- extra browser permissions;
- message-content processing.

Those require a change request.
