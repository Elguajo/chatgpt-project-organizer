# Project Doctor

Diagnostic order:

1. run `python tools/audit.py`;
2. read `ROADMAP.md`;
3. read the current phase;
4. compare acceptance criteria with recorded evidence;
5. inspect git status/build/test output if available;
6. report the smallest next action.

Do not implement unrelated features while diagnosing project state.
