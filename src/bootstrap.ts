/**
 * Phase 00 intentionally keeps runtime inert until the live ChatGPT Project DOM
 * has been inspected and sanitized fixtures exist.
 *
 * Do not add guessed selectors here. ChatGPT-specific behavior belongs in
 * src/adapter after docs/research/LIVE_DOM_FINDINGS.md is completed.
 */
export function bootstrapProjectPins(): void {
  // Intentionally no-op during Phase 00.
}
