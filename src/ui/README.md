# UI module

Phase 03 implements:

- one extension-owned Pinned section;
- Pin/Unpin buttons on recognized native rows;
- event delegation;
- English/Russian microcopy;
- accessibility states;
- extension-owned CSS namespace.

Do not move native React nodes.
