# Audit Workflow

Check repository workflow consistency, not product feature correctness.

Run:

```bash
python tools/audit.py
```

Then inspect any reported mismatch in required files, roadmap markers, phase references, and NEXT_SESSION alignment.

Do not rewrite product scope merely to make the audit green.
