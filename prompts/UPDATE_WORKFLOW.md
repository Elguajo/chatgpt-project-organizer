# Update Workflow

1. Read `docs/system/WORKFLOW_UPDATE_POLICY.md`.
2. Compare upstream workflow behavior.
3. Never overwrite project-owned docs/source.
4. Merge useful workflow changes manually.
5. Update `.token-efficient-spec-kit/VERSION`.
6. Run `python tools/audit.py`.
7. Preserve current product handoff.
